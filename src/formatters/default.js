export default class DefaultFormatter {

    id = 'default';

    bold(text) {
        return text;
    }

    italic(text) {
        return text;
    }

    strike(text) {
        return text;
    }

}
